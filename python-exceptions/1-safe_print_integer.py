#!/usr/bin/python3
def safe_print_integer(value):
    for x in value:
    try:
        print("{:d}".format(valu[x])
        return (True)
        except (TypeError, ValueError):
            return (False)
