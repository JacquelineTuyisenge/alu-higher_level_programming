#!/usr/bin/python3
from calculator_1 import maths
if __name__ == "__main__":
    a = 10
    b = 5
    sum = a + b:
        return (sum)
    subtraction = a - b:
        return (subtraction)
    multiplication = a * b:
        return (multiplication)
    division = a / b:
        return (division)
    print("{} {} = {}".format(a, b), end="")
