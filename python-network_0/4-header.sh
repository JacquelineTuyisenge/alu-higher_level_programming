#!/bin/bash
# bash script, takes in URL as an argument, sends GET
curl sH "X-School-user-Id: 98" "$1"#!/bin/bash
# bash script, takes in URL as an argument, sends GET
curl sH "X-School-user-Id: 98" "$1"
