#!/bin/bash
# bash script that takes in URL, sends POST request to the passed URL
curl -sX POST -d email=test@gmail.com -d subject="I will always be here for PLD" "$1"#!/bin/bash
# bash script that takes in URL, sends POST request to the passed URL
curl -sX POST -d email=test@gmail.com -d subject="I will always be here for PLD" "$1"
